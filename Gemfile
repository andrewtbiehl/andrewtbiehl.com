# frozen_string_literal: true

source 'https://rubygems.org'

ruby '>= 0'

# The base64, bigdecimal, and csv gems are included to suppress warnings regarding
# their removal from the Ruby 3.4 standard library. Once other dependencies explicitly
# specify these gems as their dependencies then these can be removed as direct
# dependencies.
gem 'base64'
gem 'bigdecimal'
gem 'csv'
gem 'jekyll-feed'
gem 'jekyll-figure'
gem 'jekyll-seo-tag'
gem 'jekyll-target-blank'
gem 'kramdown-math-katex'
gem 'kramdown-parser-gfm'
gem 'kramdown-syntax_tree_sitter'
gem 'nokogiri'
